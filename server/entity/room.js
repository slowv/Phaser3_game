export default class Room {
    constructor(id, name, maxPlayer, password) {
        this.id = id;
        this.name = name;
        this.maxPlayer = maxPlayer;
        this.password = password;
    }
}
